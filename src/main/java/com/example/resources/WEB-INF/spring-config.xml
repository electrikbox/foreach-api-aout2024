<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/mvc
                           http://www.springframework.org/schema/mvc/spring-mvc.xsd">   
                           

    <!-- Enable Spring MVC annotations like @RequestMapping -->
    <mvc:annotation-driven/>

    <bean id="config" class="com.example.config.AppConfig"/>
    <!-- Ajouter le controller -->
    <bean id="etudiantController" class="com.example.controller.EtudiantController"/>

    <!-- Scan for annotated components (e.g., @Service, @Controller) -->
    <context:component-scan base-package="com.example"/>

    <!-- <context:component-scan base-package="com.example.controller" />
    <mvc:annotation-driven /> -->
</beans>
